<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.mapper.ClassMapper">

    <!-- DTO 매핑 -->
    <resultMap type="com.kh.dto.ClassDTO" id="classMap">
        <id column="class_number" property="classNumber" />
        <result column="uno" property="uno" />
        <result column="title" property="title" />
        <result column="description" property="description" jdbcType="CLOB"/>
        <result column="category" property="category" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="thumbnail" property="thumbnail" jdbcType="VARCHAR" />
        <result column="rate" property="rate" />
        <result column="name" property="name"/>
    </resultMap>

   <!-- 특정 강의 조회 -->
    <select id="selectClass" parameterType="int" resultType="com.kh.dto.ClassDTO">
        SELECT 
            c.class_number,
            c.title,
            c.description,
            c.category,
            c.create_time,
            c.update_time,
            c.thumbnail,
            c.rate,
            c.uno,
            u.name
        FROM CLASS c
        JOIN USERS u ON c.uno = u.uno
        WHERE class_number = #{classNumber}
    </select>

    <!-- 최신 강의 3개 조회 -->
    <select id="selectLatestClasses" resultMap="classMap">
        SELECT 
<<<<<<< HEAD
            c.class_number,
            c.title,
            c.description,
            c.category,
            c.create_time,
            c.update_time,
            c.thumbnail,
            c.rate,
            c.uno,
            u.name
        FROM CLASS c
        JOIN USERS u ON c.uno = u.uno
=======
            c.class_number, c.title, c.description, c.category, c.create_time,
            c.update_time, c.thumbnail, c.rate, c.uno, u.name
        FROM CLASS c JOIN USERS u ON c.uno = u.uno
>>>>>>> 751184ce14df6f290f9561852373def57bfaf751
        ORDER BY c.create_time DESC
        FETCH FIRST 3 ROWS ONLY
    </select>

    <select id="getAllClasses" resultMap="classMap">
        SELECT *
        FROM CLASS
        ORDER BY create_time DESC
    </select> 
 
    <!-- 강의목록 조회 카테고리 및 정렬 조건을 적용 -->
    <select id="selectClassList" resultMap="classMap">
        SELECT c.*, u.name 
        FROM CLASS c
        JOIN USERS u ON c.uno = u.uno
        <where>
            <if test="category != null and category != '전체'">
                AND c.category = #{category}
            </if>
        </where>
        <choose>
            <when test="sort == '최신순'">
<<<<<<< HEAD
                ORDER BY c.create_time DESC
            </when>
            <when test="sort == '평점순'">
                ORDER BY c.rate DESC
            </when>
            <otherwise>
                ORDER BY c.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 강의 검색 기능 -->
    <select id="searchClasses" resultMap="classMap">
        SELECT 
            c.class_number, 
            c.title, 
            c.description, 
            c.category, 
            c.create_time, 
            c.update_time, 
            c.thumbnail, 
            c.rate, 
            c.uno, 
            u.name
        FROM CLASS c
        JOIN USERS u ON c.uno = u.uno
        <where>
            <if test="searchKeyword != null and searchKeyword != ''">
                AND (LOWER(c.title) LIKE '%' || LOWER(#{searchKeyword}) || '%')
            </if>
            <if test="category != null and category != '전체'">
                AND c.category = #{category}
            </if>
        </where>
        <choose>
            <when test="sort = '최신순'">
=======
>>>>>>> 751184ce14df6f290f9561852373def57bfaf751
                ORDER BY c.create_time DESC
            </when>
            <when test="sort == '평점순'">
                ORDER BY c.rate DESC
            </when>
            <otherwise>
                ORDER BY c.create_time DESC
            </otherwise>
        </choose>
    </select>

<<<<<<< HEAD
    <select id="selectClassNo" resultType="int">
        SELECT seq_class_class_number.NEXTVAL FROM dual
    </select>

    <insert id="insertClass" parameterType="com.kh.dto.ClassDTO">
        INSERT INTO CLASS (class_number, title, description, category, create_time, update_time, thumbnail, rate, uno)
        VALUES (#{classNumber}, #{title}, #{description}, #{category}, SYSDATE, SYSDATE, #{thumbnail}, 0, #{uno})
    </insert>

=======
    <select id="searchClasses" resultMap="classMap">
        SELECT c.class_number, c.title, c.description, c.category, 
            c.create_time, c.update_time, c.thumbnail, c.rate, 
            c.uno, u.name
        FROM CLASS c
        JOIN USERS u ON c.uno = u.uno
        <where>
            <if test="searchKeyword != null and searchKeyword != ''">
                AND (LOWER(c.title) LIKE '%' || LOWER(#{searchKeyword}) || '%')
            </if>

            <if test="category != null and category != '전체'">
                AND c.category = #{category}
            </if>
        </where>
        <if test="sort eq '평점순'">
            ORDER BY c.rate DESC
        </if>
        <if test="sort eq '최신순' or sort == null">
            ORDER BY c.create_time DESC
        </if>
    </select>

    <select id="selectClassNo" resultType="int">
        SELECT seq_class_class_number.NEXTVAL FROM dual
    </select>

    <insert id="insertClass" parameterType="com.kh.dto.ClassDTO">
        INSERT INTO CLASS (class_number, title, description, category, create_time, update_time, thumbnail, rate, uno)
        VALUES (#{classNumber}, #{title}, #{description}, #{category}, SYSDATE, SYSDATE, #{thumbnail}, 0, #{uno})
    </insert>

>>>>>>> 751184ce14df6f290f9561852373def57bfaf751


</mapper>